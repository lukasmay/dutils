services:
  web:
    image: nginx:alpine
    ports:
      - "18080:80"

  db:
    image: postgres:alpine
    environment:
      POSTGRES_PASSWORD: testpassword

  worker:
    image: busybox
    command: ["sleep", "infinity"]
